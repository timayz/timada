---
description: Run a technical + on-page SEO review and apply safe fixes
argument-hint: [optional scope — route, template, or page]
---

Use the `seo-reviewer` agent to audit technical + on-page SEO and apply safe
in-place fixes. Flag higher-risk changes (indexability policy, canonical
strategy, URL renames) before touching them.

Scope: $ARGUMENTS

If no scope is given, review the site-wide head/layout and the most recently
changed routes.
